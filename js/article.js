const articleCanvas=document.querySelector('.article-canvas'),articleCtx=articleCanvas.getContext('2d'),articleParticles=[];function sizeArticleCanvas(){const d=devicePixelRatio||1;articleCanvas.width=innerWidth*d;articleCanvas.height=innerHeight*d;articleCtx.setTransform(d,0,0,d,0,0)}document.addEventListener('pointerdown',event=>{for(let i=0;i<14;i++){const a=Math.random()*Math.PI*2,s=Math.random()*2.8+1;articleParticles.push({x:event.clientX,y:event.clientY,vx:Math.cos(a)*s,vy:Math.sin(a)*s,life:1,color:['#f4793f','#ffb72b','#8ebf8b'][i%3]})}});function animateArticle(){articleCtx.clearRect(0,0,innerWidth,innerHeight);articleParticles.forEach(p=>{p.x+=p.vx;p.y+=p.vy;p.vy+=.03;p.life-=.022;articleCtx.globalAlpha=Math.max(0,p.life);articleCtx.fillStyle=p.color;articleCtx.beginPath();articleCtx.arc(p.x,p.y,3,0,Math.PI*2);articleCtx.fill()});for(let i=articleParticles.length-1;i>=0;i--)if(articleParticles[i].life<=0)articleParticles.splice(i,1);articleCtx.globalAlpha=1;requestAnimationFrame(animateArticle)}addEventListener('resize',sizeArticleCanvas);sizeArticleCanvas();animateArticle();
